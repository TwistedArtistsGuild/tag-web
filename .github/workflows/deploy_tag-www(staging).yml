# Staging deployment workflow intentionally disabled.
#
# There is currently no Azure App Service staging slot for tag-www.
# The staging slot was removed / is not provisioned at this time due to cost constraints.
#
# If staging is needed again in the future:
# 1. Recreate the Azure App Service staging slot.
# 2. Restore or recreate the staging publish profile / federated credentials.
# 3. Re-enable this workflow with the correct build and deploy steps.
#
# This file is kept as a placeholder so the lack of a staging workflow is explicit.